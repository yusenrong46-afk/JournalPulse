# Experiment Artifacts

Store measured JSON results in `simulator/`, `policies/`, `llm/`, `retrieval/`, or `safety/`.

Every artifact should include `experiment_id`, UTC timestamp, git commit, seed set, configuration,
metrics with uncertainty, and artifact provenance. Never place raw journal text, credentials, or mock
results presented as evidence here.
